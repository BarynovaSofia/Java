📘 Шпаргалка: Как тестировать многопоточность без боли

---

### 🔄 Почему тесты в многопоточности сложные?

1. **Непредсказуемость порядка выполнения**: потоки могут выполняться в разной последовательности при каждом запуске.
2. **Асинхронность**: результат работы может быть доступен не сразу.
3. **Состояния, зависящие от времени**: `Thread.sleep()`, ожидание завершения, таймауты.
4. **Параллельные ошибки**: ошибки могут быть неочевидны и не повторяться каждый раз.
5. **Тесты требуют управления зависимостями**: часто приходится выносить логику, чтобы протестировать её изолированно.

---

### 🧪 Что именно тестировать в многопоточном коде?

#### ✅ Основные цели тестов:

* Проверка логики внутри `Runnable` или `Callable`
* Проверка корректного завершения задач
* Проверка взаимодействия между потоками (если есть)
* Проверка обработки исключений и остановки
* Проверка состояний и счётчиков (например, `AtomicInteger`)

---

### 🔧 Стратегии тестирования

#### 1. **Вынос логики из main()**

* Всё, что не относится к запуску, лучше вынести в отдельный метод/класс.

#### 2. **Имитация времени**

* Вместо `Thread.sleep()` — внедрять интерфейс `Sleeper`, чтобы мокать паузу.

#### 3. **Контроль через моки**

* `ExecutorService`, `Sleeper`, счётчики, зависимые сервисы — всё это можно мокать с помощью Mockito.

#### 4. **Изолированное тестирование задач**

* `HomeworkCheckTask` или любая логика внутри потока должна тестироваться отдельно, без запуска через `ExecutorService`.

#### 5. **Интеграционные тесты с `ExecutorService`**

* Проверяем запуск задач, завершение по `shutdown()` и `awaitTermination()`.

---

### 📋 Чеклист: что включать в тесты

* [x] Логика внутри Runnable/Callable (print, счётчик, проверка данных)
* [x] Обработка `InterruptedException`
* [x] Моки и проверки через `verify()`
* [x] Проверка, был ли вызван `shutdown()`/`shutdownNow()`
* [x] Проверка `awaitTermination()` при истечении времени
* [x] Проверка поведения при незавершённых задачах

---

### 💡 Советы

* Не бойся разбивать код на большее количество классов — чем меньше ответственность у класса, тем проще тесты.
* Вводи интерфейсы или поставщиков зависимостей (как `Sleeper`) — это не «избыточность», а путь к тестируемости.
* Логика != запуск. Разделяй, и будешь править в тестах 😎

---

### ✨ Пример

Если у тебя есть `Runnable`, который спит 5 секунд, а потом печатает сообщение, то в тесте:

* Создай мок `Sleeper`
* Проверь, что он вызван с нужным временем
* Убедись, что сообщение напечатано (если нужно, можешь читать `System.out` через `ByteArrayOutputStream`)

---